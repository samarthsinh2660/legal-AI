import { AuditTrail } from "@/features/audit/component/audit-trail";

/** The audit trail, in the reader's language: "Activity". */
export default function ActivityPage() {
  return <AuditTrail />;
}
