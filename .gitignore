__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
data/
.env
problems.md
.claude/
backups/

# Resumable fetch caches for one-off repair scripts -- regenerate with the
# script's own `fetch` phase rather than versioning a megabyte of derived
# archive metadata.
scripts/sci_pdf_paths.json
